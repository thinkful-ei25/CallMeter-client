import React from 'react';
import {shallow, mount} from 'enzyme';


import RecentCalls from '../../../components/contacts/RecentCalls';

describe('<DeleteContact />', () => {
    it('Renders without crashing', () => {
        shallow(<RecentCalls />);
    });

    
  })